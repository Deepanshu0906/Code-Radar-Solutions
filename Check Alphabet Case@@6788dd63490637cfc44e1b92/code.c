#include <stdio.h>
#include <ctype.h>
int main(){
    char alpha;
    scanf("%c"&aplha);
    if(islower(alpha)){
        printf("Uppercase");
    }
    else{
        printf("Lowercase")
    }
}